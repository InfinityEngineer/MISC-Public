# SSH Status Report from OptiPlex 9010

Generated by Claude Code running on the OptiPlex. Updated after applying fixes.

## CRITICAL FINDINGS

1. **The Windows username is `clayi`, NOT `clay`.** Connect with: `ssh clayi@192.168.1.195`
2. **There are TWO user profile folders**: `C:\Users\clay` and `C:\Users\clayi`. The actual account is `clayi`. The earlier key was written to the wrong profile.

## WHAT I FIXED

- Recreated `C:\ProgramData\ssh\administrators_authorized_keys` with no BOM (UTF-8 clean)
- Created `C:\Users\clayi\.ssh\authorized_keys` (correct profile) with proper permissions
- Fixed permissions on per-user key: only clayi, SYSTEM, Administrators have access
- Re-enabled `Match Group administrators` in sshd_config so BOTH key files are checked
- Restarted sshd

## Current State

- **sshd**: Running, Automatic
- **Listening**: 0.0.0.0:22
- **Firewall**: Inbound allow rule active
- **IP**: 192.168.1.195 (Ethernet)

## Key Files (both have correct content, no BOM)

- `C:\ProgramData\ssh\administrators_authorized_keys` — perms: SYSTEM:(F), Administrators:(F)
- `C:\Users\clayi\.ssh\authorized_keys` — perms: clayi:(F), SYSTEM:(F), Administrators:(F)

## sshd_config (relevant)

```
AuthorizedKeysFile .ssh/authorized_keys
Match Group administrators
   AuthorizedKeysFile __PROGRAMDATA__/ssh/administrators_authorized_keys
```

## TRY THIS FROM THE RAZER

```bash
ssh clayi@192.168.1.195
```

If that still fails, try with verbose to see what's happening:
```bash
ssh -vvv clayi@192.168.1.195
```

And paste the output into a new file in this repo so the OptiPlex CC instance can read it.

## If key auth still fails — enable password auth as fallback

Push updated instructions to OPTIPLEX_DO_THIS.md with this command for me to run:
```powershell
(Get-Content C:\ProgramData\ssh\sshd_config) -replace '#PasswordAuthentication yes','PasswordAuthentication yes' | Set-Content C:\ProgramData\ssh\sshd_config; Restart-Service sshd
```
