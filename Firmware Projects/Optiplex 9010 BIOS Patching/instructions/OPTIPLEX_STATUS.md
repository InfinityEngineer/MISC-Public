# SSH Status Report from OptiPlex 9010

Generated by Claude Code running on the OptiPlex.

## CRITICAL FINDING

**The Windows username on this machine is `clayi`, NOT `clay`.**

The only enabled local account is `clayi`. You must SSH as:

```
ssh clayi@192.168.1.195
```

NOT `ssh clay@192.168.1.195`.

## Service Status

- **sshd**: Running, StartType = Automatic
- **Listening**: TCP 0.0.0.0:22 (confirmed via netstat)
- **Firewall**: OpenSSH SSH Server inbound rule is Enabled, Action = Allow

## IP Address

- **Ethernet**: 192.168.1.195
- **Tailscale**: 169.254.83.107

## Key Files

### C:\ProgramData\ssh\administrators_authorized_keys
- **Content**: `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIFd7PqcOPCAJR0gnleLWjl9TGMQvr2ZoXDKkr5QVM8OL clayi@Nostalgia-for-Infinity`
- **Encoding**: UTF-8, NO BOM (first bytes: 115 115 104 = "ssh")
- **Permissions**: BUILTIN\Administrators:(F), NT AUTHORITY\SYSTEM:(F) — no inherited ACLs

### C:\Users\clayi\.ssh\authorized_keys
- **Content**: Same key as above
- **Permissions**: Has inherited ACLs (BUILTIN\Users, Everyone have RX) — this may cause sshd to reject it due to loose permissions

## sshd_config (relevant lines)

```
#Port 22
#PubkeyAuthentication yes        (commented = defaults to yes)
AuthorizedKeysFile .ssh/authorized_keys
#Match Group administrators      (COMMENTED OUT — won't use admin key override)
#   AuthorizedKeysFile __PROGRAMDATA__/ssh/administrators_authorized_keys
```

## PROBLEM ANALYSIS

Since `Match Group administrators` is commented out, sshd will ONLY look at `C:\Users\clayi\.ssh\authorized_keys` (the per-user file). But that file has loose inherited permissions (Everyone can read it), which sshd's StrictModes may reject silently.

## RECOMMENDED FIX (for the Razer CC instance to tell the user, or for next check-in)

Option A — Fix per-user key permissions:
```powershell
icacls C:\Users\clayi\.ssh\authorized_keys /inheritance:r /grant "clayi:(F)" /grant "NT AUTHORITY\SYSTEM:(F)" /grant "BUILTIN\Administrators:(F)"
```

Option B — Re-enable the admin key override in sshd_config:
```powershell
(Get-Content C:\ProgramData\ssh\sshd_config) -replace '^#Match Group administrators','Match Group administrators' -replace '^#\s*AuthorizedKeysFile __PROGRAMDATA__','   AuthorizedKeysFile __PROGRAMDATA__' | Set-Content C:\ProgramData\ssh\sshd_config
Restart-Service sshd
```

Option C — Just try the correct username first:
```bash
ssh clayi@192.168.1.195
```

**Try Option C first.** The username being wrong is the most likely cause of failure.
